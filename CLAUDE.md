# CLAUDE.md

## 项目定位

本项目是跨境收单支付系统后端，涉及商户 OpenAPI、支付交易、代付、渠道回调、对账、清分、结算、风控、后台管理等模块。修改代码时必须优先保证资金安全、状态一致、幂等、防重放、加密和日志脱敏。

## 高优先级规则

1. 不要一次性重构全仓库。
2. 不要修改未被要求修改的业务代码。
3. 不要随意修改接口路径、字段、签名规则、加密规则、状态码。
4. 不要绕过 `@VerificationAndProcessing` 新增商户对外接口。
5. 不要把支付核心逻辑写在 Controller。
6. 不要把具体业务逻辑放进 `component-library`。
7. 不要在模拟支付或模拟代付实现上继续堆复杂业务。
8. 不要使用 `double` / `float` 处理金额、费率、汇率。
9. 不要在日志中输出完整卡号、CVV、JWT、API Key、私钥、完整证件号。
10. 不要生成大量模板化注释和临时 Markdown 报告。
11. 不要修改 SQL、YAML、POM，除非任务明确要求。
12. 修改资金、状态、幂等、回调、加密相关代码前，必须先说明风险和验证方式。

## 输出要求

修改前说明：

- 准备修改模块
- 准备修改文件
- 修改原因
- 风险点
- 验证方式

修改后说明：

- 已修改文件
- 修改摘要
- 是否影响接口
- 是否影响数据库
- 是否影响配置
- 是否影响 MQ / 定时任务
- 建议测试

