# ARCH-REVIEW-001 待用户确认决策

> 状态说明（2026-08-02）：本文记录迁移前决策过程；D-001 至 D-003、D-025 等自研分表/locator 决策已由第一版 ShardingSphere 直接重构结论覆盖。
> 未完成的环境验收项以 `07-test-and-acceptance-matrix.md` 第 5.9 节起的记录为准。

| 决策编号 | 问题 | 当前实现 | 可选方案 | 推荐方案 | 风险 | 等待确认 |
|---|---|---|---|---|---|---|
| D-001 | 短期保留自研分表还是切换 ShardingSphere | 当前主路径是自研应用层路由，非 ShardingSphere | A. 保留自研并收口 Repository；B. 立即切 ShardingSphere；C. 双轨 PoC | A，另立 PoC 评估 B | 立即切换风险高，双轨会拉长周期 | 请确认短期是否保留自研分表 |
| D-002 | 永久分片定位表是否建设为单表还是分表 | 当前没有 locator | A. 单表；B. 按季度分表；C. 单表 + 后续归档 | A 起步，数据量达阈值后 C | 分表 locator 会重新引入定位问题 | 请确认 locator 初始形态 |
| D-003 | locator 历史回填范围 | 当前依赖交易号解析和部分全历史扫描 | A. 全量历史；B. 从 2026 Q3 起；C. 只新交易双写，旧交易按需回填 | B 起步，关键历史按需补 | 全量回填成本和风险更高 | 请确认回填范围 |
| D-004 | 风控远程调用异常/超时策略 | 远程调用异常抛 `BAD_GATEWAY`，Noop 默认可能跳过 | A. fail-closed 失败；B. fail-pending/PROCESSING；C. REVIEW 人工复核；D. fail-open | 高风险交易 A/C，低风险可 C；不推荐 D | 策略影响交易转化率和合规 | 请确认各交易类型异常策略 |
| D-005 | Noop 风控允许的环境 | `matchIfMissing=true`，基础配置默认 false | A. 仅 local/test；B. dev 可用；C. UAT/Prod 禁止启动 | C + local/test 限制 | 配置缺失会绕过风控 | 请确认 Noop 环境边界 |
| D-006 | 后续交易风控覆盖范围 | Capture/Refund/Void/Incremental 未调用风控 | A. 全量执行风控；B. scoped risk；C. 只退款和增量授权；D. 不做 | B | 全量可能重复检查，过窄会漏风险 | 请确认每类后续交易规则范围 |
| D-007 | AML 是否允许白名单或 VIP 绕过 | 当前运行时未接入 | A. 不允许；B. 特定审批白名单可绕过；C. VIP 可绕过 | A，审批例外单独建机制 | 合规风险高 | 请确认 AML 强制策略 |
| D-008 | VIP 是否允许绕过黑名单 | 当前运行时未接入 | A. 不允许；B. 只绕过普通规则；C. 可配置绕过黑名单 | A | 黑名单绕过可能导致资金和合规风险 | 请确认 VIP 权限边界 |
| D-009 | 风控记录表是否由 service-risk 写入 | 表和管理端查询存在，运行时未写 | A. service-risk 写；B. service-payment 写摘要；C. 双写 | A，payment 只保存引用 | 双写一致性复杂 | 请确认风控记录归属 |
| D-010 | 风控配置缓存刷新机制 | 当前未运行时读取 | A. TTL 轮询；B. Nacos/消息推送；C. 管理端变更写版本，risk 拉取 | C + TTL 兜底 | 纯 TTL 延迟生效，纯推送可靠性不足 | 请确认刷新机制 |
| D-011 | HMAC 密钥来源和轮换 | 当前普通 SHA-256 | A. K8s Secret/env；B. Nacos 加密配置；C. KMS；D. DB 存密钥 | C 最佳，A 可起步；不推荐 D | 密钥泄露会影响全部名单匹配 | 请确认密钥管理方式 |
| D-012 | HMAC 历史迁移是否保留 SHA-256 双读 | 当前名单表有 SHA-256 | A. 双读过渡；B. 一次性全量 HMAC 后关闭 SHA；C. 永久保留 SHA | A | 永久 SHA 增加泄露风险，一次性切换风险高 | 请确认迁移策略 |
| D-013 | 风控字段最小采集范围 | 当前只传有限字段 | A. 覆盖所有已建规则；B. 只覆盖 P0 规则；C. 按商户产品分批 | A 设计，按 C 灰度 | 字段采集涉及敏感数据合规 | 请确认字段范围 |
| D-014 | 流程事件 detailJson 大小 | 当前无 detailJson | A. 1KB；B. 4KB；C. 16KB；D. 不使用 JSON | B | 过大影响表体积，过小排障不足 | 请确认大小上限 |
| D-015 | 流程事件是否保留旧批量事件 | 当前批量补写 | A. 新旧双写后关闭旧；B. 立即替换；C. 永久保留 | A | 立即替换影响详情页，永久保留会继续混淆 | 请确认灰度方式 |
| D-016 | 事件失败是否影响主交易 | 当前事件写入在交易事实记录中，失败可能影响主流程 | A. 关键事件影响；B. 所有事件不影响；C. 按事件类型配置 | C | 所有不影响会丢关键审计，所有影响会降低可用性 | 请确认事件事务策略 |
| D-017 | 回调验签失败是否写交易流程事件 | 当前安全拦截事件在独立安全表，不一定有交易号 | A. 只写安全表；B. 能定位交易时也写 flow event；C. 全部写 flow event | B | 无法定位交易时强写会污染时间线 | 请确认回调安全事件归属 |
| D-018 | MQ 和商户通知事件粒度 | 当前只在专表/日志 | A. 每次尝试都写 flow event；B. 只写摘要和最终结果；C. 只写失败 | A 对审计最好，B 对数据量更稳 | A 事件量上升 | 请确认异步事件粒度 |
| D-019 | 普通查询最大跨度 | 当前未统一限制 | A. 31 天；B. 90 天；C. 180 天；D. 不限制 | B，后台导出走异步 | 跨度太短影响运营，太长影响性能 | 请确认默认跨度 |
| D-020 | 风控 REVIEW 状态映射 | 当前状态机未明确人工复核状态 | A. `PROCESSING + REVIEW_PENDING`；B. 新增交易状态 `REVIEW`；C. 直接失败 | A | 新增状态影响前端和通知，直接失败影响转化 | 请确认状态字典 |
| D-021 | REQUIRE_3DS 状态映射 | 当前有 3DS 相关信息但风控骨架返回后如何串联需确认 | A. `PROCESSING + WAITING_3DS`；B. 新状态；C. 返回失败 | A | 影响 OpenAPI 响应和后续认证流程 | 请确认 3DS 交易流程 |
| D-022 | 风控 record-only 灰度期是否允许交易继续 | 当前管理端配置未生效 | A. record-only 只记录不拦截；B. 高风险立即拦截；C. 按商户灰度 | C | record-only 有风险窗口，立即拦截有误杀 | 请确认灰度策略 |
| D-023 | 生产 Nacos 当前 `payment.risk-client.remote-enabled` 真实值 | 仓库无法确认生产 DataId | A. 用户提供快照；B. 后续授权连接 Nacos 读取；C. 只按未知处理 | A 或 B | 无法证明生产风控已开启 | 请提供或授权核对配置 |
| D-024 | 是否纳入 checkout 风控同源治理 | Hosted Checkout 有独立 IP/source/device hash | A. 纳入本次；B. 下个专项；C. 只复用公共字段 | C 起步 | checkout 和 OpenAPI 风控口径不一致 | 请确认 checkout 范围 |
| D-025 | 是否允许后续整改修改数据库 DDL | 本轮禁止 DDL 修改 | A. 下轮授权可新增向后兼容 DDL；B. 只设计不落库；C. 先应用层绕开 | A | 无 DDL 无法彻底解决 locator/事件字段 | 请确认下轮 DDL 授权边界 |

## 2. 优先需要确认的决策

| 优先级 | 决策编号 | 原因 |
|---|---|---|
| P0 | D-004 | 风控异常策略会影响所有风控接入后的交易结果 |
| P0 | D-005 | Noop 是否允许存在决定生产绕过风险 |
| P0 | D-006 | 后续交易风控范围决定 R-001 的修复边界 |
| P0 | D-007 | AML 绕过策略是合规核心 |
| P0 | D-025 | 无 DDL 授权无法建设 locator 和事件字段 |
| P1 | D-001 | 分表方案方向影响后续所有代码改造 |
| P1 | D-011 | HMAC 密钥来源影响敏感数据设计 |
| P1 | D-015 | 新旧事件并行策略影响回归范围 |
