global-payment:
  sharding:
    payment-order:
      logical-table: payment_order
      actual-data-nodes: ds_${0..1}.payment_order_${0..15}
      sharding-column: merchant_id
      table-count: 16
    payout-order:
      logical-table: payout_order
      actual-data-nodes: ds_${0..1}.payout_order_${0..15}
      sharding-column: merchant_id
      table-count: 16

