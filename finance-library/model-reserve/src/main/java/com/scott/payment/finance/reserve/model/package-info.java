/**
 * 保证金扣留和返还的不可变契约。
 *
 * <p>本包只表达标签币种保证金的 HOLD、RETURN、原支付固化比例和累计返还上限。保证金清分事实与交易费用
 * 明细独立，退款只能追加返还事实，不能覆盖原扣留记录。</p>
 *
 * <p>禁止出现汇率、结算币种、资金账户余额、数据库实体、缓存和消息消费逻辑。</p>
 */
package com.scott.payment.finance.reserve.model;
