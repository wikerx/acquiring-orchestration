/**
 * 财务领域最底层金额模型。
 *
 * <p>本包只负责不可变金额、ISO 4217 币种代码和业务事实固化的 exponent。金额允许为负，以支持净额、
 * 调整额和后续结算差额；费用、保证金等上层领域必须在自己的命令模型中校验金额方向。</p>
 *
 * <p>禁止放入汇率、费用规则、保证金规则、数据库实体、Spring Bean、缓存或消息契约。</p>
 */
package com.scott.payment.finance.money.model;
