/**
 * 保证金领域纯计算内核。
 *
 * <p>本包仅按标签金额、原支付固化保证金比例和舍入规则计算 HOLD、RETURN 事实，并保证累计返还不超过
 * 原扣留金额。计算结果由清分服务持久化到独立保证金明细。</p>
 *
 * <p>禁止处理汇率、结算批次、资金账户、保证金释放任务、数据库、缓存和消息消费。</p>
 */
package com.scott.payment.finance.reserve.core;
