<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.scott.payment</groupId>
        <artifactId>component-library</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>component-job</artifactId>
    <packaging>jar</packaging>
    <name>component-job</name>
    <description>调度任务 Handler、执行上下文、结果模型和注册契约组件。</description>

    <!--本模块只定义任务执行公共契约；任务持久化、节点选举和具体补偿编排属于 service-job。-->
    <dependencies>
        <!--复用任务状态枚举、异常、ID 和通用结果模型。-->
        <dependency>
            <groupId>com.scott.payment</groupId>
            <artifactId>component-core</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
</project>
